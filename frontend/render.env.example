# Variáveis de build do Vite. O navegador usa o mesmo domínio do frontend.
VITE_API_BASE_URL=/api
VITE_SOCKET_URL=

# Variável de runtime do Nginx. O Blueprint injeta este hostport
# automaticamente; não fixe o hash de uma instância em código.
API_UPSTREAM=api-xxxx:3000
